[pytest]
testpaths = server/tests
asyncio_mode = auto
filterwarnings =
    # Expected: tools tests mock the event loop, so _execute_async is created but never run.
    ignore:coroutine.*was never awaited:RuntimeWarning
